pip install python-decouple

just like .env
create .env file
then go to setting.py and add secert key in top . same in env file also
genrertae secret key from djecrety website and paste in .env


git setup -> what to send or what not

go to gitignore.io site. and select django and react it will generate a complete gitignore file.